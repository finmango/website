# Clean URL redirects - remove .html extensions
# This file works with Netlify and Cloudflare Pages

# Redirect .html URLs to clean URLs (301 permanent redirect)
/*.html /:splat 301

# Serve clean URLs by looking for .html files
/* /:splat.html 200
